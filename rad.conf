# /etc/rad.conf initial (minimal)
interface ix0 { }

# /etc/rad.conf (with DNS advertisement after acquiring delegated prefix)
#interface ix0 {
#    dns {
#        nameserver fd00:AAAA:BBBB:CCCC::1
#    }
#}
